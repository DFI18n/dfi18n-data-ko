base = "adv::mount"

[[rulesets]]
[rulesets.rules]
"Mount {name}{direction}" = "{name}{direction} 타기"

[[rulesets]]
name = "name"
[rulesets.rules]
"{::creatures::caste::name}" = "{::creatures::caste::name}"

[[rulesets]]
name = "direction"
optional = true
[rulesets.rules]
"{::adv::direction}" = "{::adv::direction}"