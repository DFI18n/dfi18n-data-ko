base = "adv::lead"

[[rulesets]]
[rulesets.rules]
"Lead {name}{direction}" = "{name}{direction} 끌기"

[[rulesets]]
name = "name"
[rulesets.rules]
"{::creatures::caste::name}" = "{::creatures::caste::name}"

[[rulesets]]
name = "direction"
optional = true
[rulesets.rules]
"{::adv::direction}" = "{::adv::direction}"